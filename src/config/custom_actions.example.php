<?php

/**
 * Add all queries you wish to execute into the $queries array.
 */

$queries = array(); // Don't touch this

$queries += array('Blah');
$queries += array('Blah');
$queries += array('Blah');

?>
